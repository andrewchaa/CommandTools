using System;

namespace NTail.Helpers
{
    public static class StringHelpers
    {
        public static bool Contains(this string source, string toCheck, StringComparison comp)
        {
            return source.IndexOf(toCheck, comp) >= 0;
        }
    }
}